# Imagen base de NGINX
FROM nginx:latest

# Copiar archivos del frontend (HTML, CSS, JS)
COPY ../webapp /usr/share/nginx/html

# Reemplazar configuración de NGINX
COPY ./nginx.conf /etc/nginx/nginx.conf

# Exponer puerto 80
EXPOSE 80
